# Frontend environment variables
# Prefer same-origin '/api' via platform rewrites (Vercel) to avoid CORS.
# If you must point to a full origin, include '/api' (e.g., https://backend.example.com/api)
VITE_API_URL=/api

# Toggle mock data for unknown endpoints while keeping UI functional
VITE_USE_MOCKS=false
VITE_API_URL=http://localhost:3000
VITE_API_TIMEOUT=30000
VITE_ENABLE_ANALYTICS=false
